# 📝 To-Do List Manager (Python)

A simple, interactive, terminal-based To-Do List application written in Python.  
It supports adding, viewing, editing, deleting, and saving tasks. Tasks are automatically loaded from a local file when the program starts.

---

## 🚀 Features

✔ Add new tasks  
✔ View all tasks  
✔ Edit an existing task  
✔ Delete a task  
✔ Clear all tasks  
✔ Save tasks to file  
✔ Automatically loads previous tasks at startup  
✔ Optional colored text (Linux & Mac; Windows uses no color fallback)

---

## ▶️ How to Run

1. Make sure you have **Python 3** installed.  
2. Download or clone this repository.  
3. Open your terminal or command prompt in the project folder.  
4. Run the program using:

```bash
python "The To-Do List Manager.py"

---


